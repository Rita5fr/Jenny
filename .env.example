# ============================================
# Environment
# ============================================
ENV=local

# ============================================
# OpenAI API Configuration
# ============================================
OPENAI_API_KEY=your-openai-api-key-here

# ============================================
# Mem0 Open Source Configuration (Local)
# ============================================
# Using mem0ai open source library (NOT cloud service)
# Configured to use local PostgreSQL + pgvector and Neo4j
MEMO_BASE_URL=http://127.0.0.1:8081
MEMO_EMBED_MODEL=text-embedding-3-small

# Mem0 Storage Configuration (all local)
MEM0_VECTOR_STORE=pgvector      # Local PostgreSQL with pgvector
MEM0_GRAPH_STORE=neo4j          # Local Neo4j graph database
MEM0_EMBEDDER=openai            # OpenAI embeddings (API key above)

# ============================================
# Postgres Configuration (Local via Docker)
# ============================================
# Using local PostgreSQL with pgvector (open-source alternative to Supabase)
# Default credentials match docker-compose.yml
PGHOST=localhost
PGPORT=5432
PGDATABASE=jenny_db
PGUSER=jenny
PGPASSWORD=jenny123
PGSSLMODE=disable

# Alternative: If connecting to Supabase cloud (not recommended, use local instead):
# PGHOST=your-project-ref.supabase.co
# PGDATABASE=postgres
# PGUSER=postgres
# PGPASSWORD=your-supabase-password
# PGSSLMODE=require
# PGCHANNELBINDING=require

# ============================================
# Redis Configuration (Local via Docker)
# ============================================
# Redis is used for:
# - APScheduler job persistence (scheduled reminders)
# - Caching (session state, temporary data)
# - Mem0 can optionally use Redis for caching
#
# If using docker-compose (recommended):
REDIS_URL=redis://localhost:6379

# Alternative: If you installed Redis manually with password:
# REDIS_URL=redis://:your-password@localhost:6379

# ============================================
# Neo4j Configuration (Local via Docker)
# ============================================
# If using docker-compose (default password: jenny123):
NEO4J_URI=neo4j://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=jenny123

# Alternative: If you installed Neo4j Desktop:
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=your-neo4j-password

# ============================================
# Calendar Integrations
# ============================================
# Google Calendar API
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CALENDAR_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CALENDAR_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALENDAR_REDIRECT_URI=http://localhost:8044/auth/google/callback

# Microsoft Outlook/Graph API
# Get from: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps
MICROSOFT_CLIENT_ID=your-microsoft-client-id
MICROSOFT_CLIENT_SECRET=your-microsoft-client-secret
MICROSOFT_REDIRECT_URI=http://localhost:8044/auth/microsoft/callback

# Apple Calendar (iCloud CalDAV)
# Note: Requires app-specific password from https://appleid.apple.com
APPLE_CALENDAR_ID=user@icloud.com
APPLE_CALENDAR_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# ============================================
# Messaging Platform
# ============================================
# Telegram Bot (for all notifications, reminders, and user interactions)
# Get from: https://t.me/BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# ============================================
# Scheduler Configuration
# ============================================
# Timezone for scheduled reminders (default: UTC)
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
SCHEDULER_TIMEZONE=America/New_York

# ============================================
# Security & Authentication
# ============================================
# JWT Secret Key (generate with: openssl rand -hex 32)
JWT_SECRET_KEY=your-jwt-secret-key-here-generate-with-openssl
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=1440

# Encryption Key for end-to-end encryption
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-encryption-key-here-generate-with-python

# ============================================
# Dashboard & Premium Features
# ============================================
DASHBOARD_ENABLED=true
PREMIUM_FEATURES_ENABLED=true

# ============================================
# Optional AI Services
# ============================================
# Anthropic Claude API (optional - for advanced reasoning)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# Google Gemini API (for Gemini models and voice transcription)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key
GOOGLE_API_KEY=your-google-api-key  # Same as GEMINI_API_KEY, for langchain-google-genai

# DeepSeek API (for DeepSeek Chat models)
# Get from: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your-deepseek-api-key

# ============================================
# LLM Model Configuration (CrewAI & Mem0)
# ============================================
# This section configures which LLM models to use for different components

# CrewAI Agent LLM Provider
# Options: openai, deepseek, gemini
# This determines which LLM powers Jenny's CrewAI agents
CREWAI_LLM_PROVIDER=deepseek

# Model names for each provider (only the selected provider is used)
OPENAI_MODEL=gpt-4o-mini            # OpenAI model for CrewAI agents
DEEPSEEK_MODEL=deepseek-chat        # DeepSeek model for CrewAI agents (recommended for cost)
GEMINI_MODEL=gemini-2.0-flash-exp   # Gemini model for CrewAI agents (fast and capable)

# Mem0 LLM Configuration (for reading/retrieving memories)
# Options: openai, deepseek
# This LLM is used when Mem0 needs to understand and retrieve memories
MEM0_LLM_PROVIDER=deepseek
MEM0_LLM_MODEL=deepseek-chat        # Model for memory operations (reading/retrieving)

# Mem0 Embeddings Configuration (for saving memories)
# This is used to create embeddings when storing new memories
# Recommended: text-embedding-3-small (OpenAI, cost-effective and high quality)
MEM0_EMBED_MODEL=text-embedding-3-small

# ============================================
# Model Selection Guide
# ============================================
# CrewAI Agents (CREWAI_LLM_PROVIDER):
#   - deepseek: Best cost/performance ratio, good for most tasks
#   - gemini: Fast and capable, good for real-time interactions
#   - openai: Most reliable, best for complex reasoning
#
# Mem0 (MEM0_LLM_PROVIDER):
#   - deepseek: Recommended for cost savings on memory operations
#   - openai: More reliable but more expensive
#
# Embeddings (MEM0_EMBED_MODEL):
#   - Always use text-embedding-3-small (OpenAI)
#   - This is cost-effective and high quality for semantic search

# ============================================
# Monitoring & Error Tracking
# ============================================
# Sentry DSN (optional - for error tracking)
# Get from: https://sentry.io/settings/projects/
SENTRY_DSN=https://xxxxxxxx@sentry.io/xxxxxxx
SENTRY_TRACES_SAMPLE_RATE=0.1

# ============================================
# Application Settings
# ============================================
# Main application port
APP_PORT=8044

# Mem0 service port
MEM0_PORT=8081

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
